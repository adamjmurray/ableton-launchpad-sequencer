export const TRACKS = 4; // number of tracks in the device
export const PATTERNS = 8;  // number of patterns per track
export const STEPS = 64; // number of sequencer steps per pattern
export const ROWS = 8; // number of steps per row in the pattern grid
export const COLUMNS = 8; // number of steps per column in the pattern grid
export const STEP_VALUES = 5; // number of step values (off, green, yellow, orange, and red)

{
  "name": "m4l-launchpad-sequencer",
  "version": "0.6.0",
  "description": "A step sequencer for the Novation Launchpad, for use with Ableton Live and Max for Live.",
  "dependencies": {},
  "devDependencies": {
    "@babel/core": "^7.1.0",
    "@babel/preset-env": "^7.1.0",
    "@babel/register": "^7.0.0",
    "mocha": "^5.2.0",
    "rollup": "^0.66.2",
    "rollup-plugin-babel": "^4.0.3",
    "rollup-plugin-node-resolve": "^3.4.0"
  },
  "scripts": {
    "clean": "rm -f launchpad-sequencer/launchpad-sequencer.js",
    "build": "npx rollup -c",
    "dev": "npx rollup -c -w",
    "test": "npx mocha -c -r @babel/register 'tests/**/*.test.js'",
    "tdd": "npx mocha -c -w -R min -r @babel/register 'tests/**/*.test.js'"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/adamjmurray/m4l-launchpad-sequencer.git"
  },
  "author": "Adam Murray",
  "license": "BSD"
}
